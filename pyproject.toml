[project]
name = "webcrawler-root"
version = "0.1.0"
description = "Root workspace for WebCrawler"
requires-python = ">=3.13"
dependencies = [
    "langgraph-cli[inmem]>=0.4.18",
    "replicate>=1.0.7",
]

[tool.uv]
workspace = { members = ["crawler"] }
